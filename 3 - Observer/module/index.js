const { Subscriber } = require("./Subscriber");
const { Message } = require("./Message");
const { NewsletterSystem } = require("./NewsletterSystem");

module.exports = {
  Subscriber,
  Message,
  NewsletterSystem
};
